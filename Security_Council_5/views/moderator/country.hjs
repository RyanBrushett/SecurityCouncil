<!doctype html>
<html>
    <head>
        <title>{{ name }} &middot; Council</title>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/global.css">
        <link rel="stylesheet" href="/css/country.css">
    </head>
    <body>
        <header class="header">
            <h1><a href="/">Council</a></h1>
            <nav class="navigation">
                <ul>
                    <li><a href="/">Dashboard</a></li>
                    <li><a href="/logout">Logout</a></li>
                    <li><a href="/moderator/simulation/{{ simulation.getId }}">Back to {{ simulation.getName }}</a></li>
                </ul>
            </nav>
        </header>
        <main>
            <h2>{{ name }}</h2>
            {{#ambassador}}
            <p>Ambassador: {{ getName }}</p>
            {{/ambassador}}
            {{^ambassador}}
            <form>
                <p>
                    <label>Select an ambassador:</label>
                    <select>
                        {{#members}}
                        <option value="{{ getId }}">{{ getName }}</option>
                        {{/members}}
                    </select>
                </p>
            </form>
            {{/ambassador}}
            <hr class="separator">
            <h2>Members of {{ name }}</h2>
            <ul class="members">
                {{#members}}
                <li>{{ getName }}</li>
                {{/members}}
            </ul>
            <hr class="separator">
            <h2>Position paper</h2>
            <textarea disabled="true" class="textbox">We believe that...</textarea>
        </main>
    </body>
</html>
